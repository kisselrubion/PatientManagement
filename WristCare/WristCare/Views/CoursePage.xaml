<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
	xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	Title="Course"
	BindingContext="{Binding CourseViewModel,Source={StaticResource Locator}}"
	x:Class="WristCare.View.CoursePage">
	<ContentPage.Content>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="*"></RowDefinition>
				<RowDefinition Height="Auto"></RowDefinition>
			</Grid.RowDefinitions>
			<Frame HasShadow="False">
				<ListView
					x:Name="LstCourses"
					HasUnevenRows="True"
					Header="Courses"
					ItemTapped="ListView_OnItemTapped"
					SelectedItem="{Binding SelectedCourse}"
					ItemsSource="{Binding Courses}"
					Margin="5">
					<ListView.ItemTemplate>
						<DataTemplate>
							<ViewCell>
								<StackLayout Orientation="Horizontal">
									<StackLayout Orientation="Vertical" Margin="2">
										<Label Text="{Binding Title}" Margin="2"></Label>
										<Label Text="{Binding Description}" Margin="2"></Label>
									</StackLayout>
									<StackLayout Orientation="Vertical" Margin="2">
										<Label Text="{Binding CourseDate}" Margin="2"></Label>
									</StackLayout>
								</StackLayout>
							</ViewCell>
						</DataTemplate>
					</ListView.ItemTemplate>
				</ListView>
			</Frame>

			<StackLayout 
				Grid.Row="1"
				Margin="5"
				Orientation="Vertical">
				<Button
					Text="Search patients"
					Command="{Binding SearchPatientCommand}"></Button>
				<Button
					Text="Add patients"
					Command="{Binding AddPatientsCommand}"></Button>
				<Button
					Text="Add Course"
					Command="{Binding AddCourseCommand}"></Button>
			</StackLayout>
		</Grid>

	</ContentPage.Content>
</ContentPage> 